# Минимальные зависимости для запуска проекта.
#
# Установка:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Для GPU-обучения: заменить torch на CUDA-версию ПЕРЕД установкой остального:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.3.0           # YOLOv8 / v11 + ByteTrack
opencv-python>=4.9.0         # работа с изображениями и видео
PySide6>=6.7.0               # нативный GUI
numpy>=1.24.0
pandas>=2.0.0                # анализ и CSV
matplotlib>=3.7.0            # графики

# Опционально — для экспорта в ONNX:
# onnx>=1.16.0
# onnxruntime>=1.17.0
